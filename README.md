# my-devops
Knowledge of Devops ..

Devops is the combination of prople,procees and product to delivery application or service at high values.

Git & branching — clone, add/commit/push/pull, merge, rebase basics, resolve conflicts, common workflows (feature branch, trunk-based). 
IT Training Chennai

CI/CD concepts & Jenkins — pipeline stages, Jenkinsfile basics, ways to trigger build, artifacts. 
Medium

Containers (Docker) — images vs containers, Dockerfile basics, docker run, docker-compose, networking basics. 
Medium

Kubernetes basics — pods, deployments, services, configmaps, probes (readiness/liveness), basic kubectl. 
Medium

IaC & automation — Terraform basics (providers, plan/apply), Ansible playbook structure, idempotency idea. 
roadmap.sh

Linux commands & shell scripting — file ops, piping, grep/awk/sed basics, permissions, systemctl, process commands.

Build tools & packaging — Maven/Gradle basics, artifact repositories (Nexus/Artifactory).

Monitoring & logging — Prometheus/Grafana concepts, ELK stack basics (conceptual).

Cloud basics (AWS/Azure/GCP) — common services (EC2/VM, S3/Storage, IAM), basic CLI usage.

Networking basics — ports, HTTP vs HTTPS, DNS, load balancer concept, container networking.











